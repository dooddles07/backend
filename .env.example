# ============================================
# ENVIRONMENT CONFIGURATION TEMPLATE
# ============================================
# ⚠️  SECURITY NOTICE:
# 1. Copy this file to .env and fill in your values
# 2. NEVER commit .env to version control!
# 3. NEVER share .env files via email/chat/screenshots
# 4. Rotate all credentials if compromised
# ============================================

# Environment Mode
NODE_ENV=development

# Server Configuration
PORT=10000

# JWT Authentication Secret
# 🔐 Generate new secure key: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# ⚠️  CRITICAL: Change this immediately if compromised!
JWT_SECRET=your_secret_here_change_this_to_64_character_random_string

# MongoDB Connection
# 🔐 Get from: https://cloud.mongodb.com → Database Access → Create new user
# Format: mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority
MONGO_URI=your_mongodb_connection_string

# Email Service (SendGrid)
# 🔐 Get from: https://app.sendgrid.com/settings/api_keys
# ⚠️  Restrict API key permissions to "Mail Send" only
SENDGRID_API_KEY=SG.your_sendgrid_api_key_here
SENDGRID_FROM_EMAIL=your_verified_email@example.com

# Cloud Storage (Cloudinary)
# 🔐 Get from: https://cloudinary.com/console → Settings → Security
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# Frontend CORS Configuration
# 🔒 For backward compatibility (single URL)
FRONTEND_URL=http://192.168.100.6:19006

# Allowed CORS Origins (comma-separated list)
# 🔒 SECURITY: List all allowed origins for CORS
# Include all development and production URLs
# Format: http://url1:port,http://url2:port,https://production-url.com
ALLOWED_ORIGINS=http://localhost:8082,http://localhost:19006,http://localhost:3000,http://192.168.1.100:19006

# Super Admin Configuration
# 🔐 REQUIRED: Configure the default super admin account
# ⚠️  CRITICAL: Change these values in production!
SUPER_ADMIN_USERNAME=superadmin
SUPER_ADMIN_PASSWORD=your_strong_password_here_minimum_12_characters
SUPER_ADMIN_EMAIL=admin@yourdomain.com
SUPER_ADMIN_FULLNAME=Super Admin
SUPER_ADMIN_DEPARTMENT=Administration
SUPER_ADMIN_CONTACT=

# System Admin Configuration
# 🔐 REQUIRED: Used for auto-generated system messages
# This account sends automated emergency response messages
SYSTEM_ADMIN_USERNAME=resqyou_system
SYSTEM_ADMIN_PASSWORD=your_strong_system_password_here
SYSTEM_ADMIN_EMAIL=emergency@yourdomain.com
SYSTEM_ADMIN_FULLNAME=Emergency Response Team

# Optional: Admin IP Whitelist (comma-separated)
# Leave empty to allow all IPs (not recommended for production)
# ADMIN_IP_WHITELIST=192.168.100.1,192.168.100.5

# Security Settings
MAX_LOGIN_ATTEMPTS=5
LOCKOUT_DURATION=15
